import SwiftUI

struct DashboardView: View {
    var body: some View {
        ZStack {
            Color(hex: 0xFFFFFF)
                .edgesIgnoringSafeArea(.all)
            
            
        }
    }
}

#Preview {
    DashboardView()
}
